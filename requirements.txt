# Core dependencies for the Bug Bounty Toolkit test/run environment.
# Tools degrade gracefully when optional deps (androguard, statistics, ...) are missing.
httpx==0.28.1
pyyaml==6.0.3
pytest==9.1.1
pytest-cov==7.1.0
